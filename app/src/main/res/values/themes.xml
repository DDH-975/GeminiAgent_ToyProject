<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Gemini" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/operator_sky_blue</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:fontFamily">@font/jua</item>
        <item name="fontFamily">@font/jua</item>
    </style>

    <style name="Theme.Gemini" parent="Base.Theme.Gemini" />

    <!-- 일관된 카드 스타일 -->
    <style name="AppCard" parent="Widget.Material3.CardView.Elevated">
        <item name="cardBackgroundColor">@color/white</item>
        <item name="cardCornerRadius">20dp</item>
        <item name="cardElevation">4dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <!-- 버튼 스타일 -->
    <style name="ActionButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/operator_sky_blue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="cornerRadius">16dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
    </style>

    <style name="SecondaryButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/button_gray</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
    </style>

    <!-- 계산기 전용 버튼 -->
    <style name="CalculatorButton" parent="Widget.Material3.Button">
        <item name="android:layout_margin">4dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <style name="CalculatorButton.Number">
        <item name="backgroundTint">@color/button_gray</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="CalculatorButton.Operator">
        <item name="backgroundTint">@color/operator_sky_blue</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>